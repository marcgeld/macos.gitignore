# macos.gitignore

A .gitignore file for macOS

Create or add to exsisting `.gitignore`-file with:

```bash
curl -fsSL https://raw.githubusercontent.com/marcgeld/macos.gitignore/refs/heads/main/install.sh | sh
````
